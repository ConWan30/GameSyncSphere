{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npm install && echo $DATABASE_URL && node src/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
