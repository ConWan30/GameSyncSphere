{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npm install && npx prisma generate --schema=../prisma/schema.prisma && npx prisma db push --schema=../prisma/schema.prisma --accept-data-loss && node src/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
