{
  "name": "gamesyncsphere",
  "version": "1.0.0",
  "description": "Revolutionary Gaming Analytics & AI-Powered Data Monetization Platform",
  "main": "backend/src/server.js",
  "scripts": {
    "start:production": "npm run build:frontend && cd backend && node src/server.js",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start": "cd backend && node src/server.js",
    "build": "npm run build:frontend",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^6.7.0",
    "socket.io": "^4.7.1",
    "dotenv": "^1.0.0"
  }
}
